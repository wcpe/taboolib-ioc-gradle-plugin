name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  verify:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build root project
        run: .\gradlew.bat build

      - name: Build quality gate consumer
        run: .\gradlew.bat -p example :quality-gate-consumer:build

      - name: Build example consumer with explicit report-only override
        run: .\gradlew.bat -p example :consumer:build -P "taboolib.ioc.analysis.fail-on-error=false" -P "taboolib.ioc.analysis.fail-on-warning=false"

      - name: Upload test and coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            build/reports/tests/test
            build/reports/jacoco/test
            example/consumer/build/libs
            example/consumer/build/reports/taboolib-ioc
            example/quality-gate-consumer/build/reports/taboolib-ioc
          if-no-files-found: warn